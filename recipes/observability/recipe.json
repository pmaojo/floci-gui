{
  "id": "observability",
  "name": "Grafana + Prometheus",
  "description": "Full observability stack. Prometheus for metrics scraping and Grafana with pre-configured dashboard capabilities.",
  "version": "10.0.3 (Grafana) / v2.45.0 (Prometheus)",
  "accessUrl": "http://localhost:{{GRAFANA_PORT}}",
  "variables": [
    {
      "key": "GRAFANA_PORT",
      "label": "Grafana Port",
      "type": "number",
      "default": 3000,
      "description": "Port on your host machine to access the Grafana UI."
    },
    {
      "key": "PROMETHEUS_PORT",
      "label": "Prometheus Port",
      "type": "number",
      "default": 9090,
      "description": "Port to access the Prometheus UI directly."
    },
    {
      "key": "GRAFANA_ADMIN_USER",
      "label": "Grafana Admin User",
      "type": "text",
      "default": "admin",
      "description": "Username for Grafana administrator."
    },
    {
      "key": "GRAFANA_ADMIN_PASSWORD",
      "label": "Grafana Admin Password",
      "type": "password",
      "default": "admin",
      "description": "Password for Grafana administrator."
    }
  ]
}
